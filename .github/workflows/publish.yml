name: semantic release

on:
  push:
    branches:
      - 'main'

jobs:
  check-component-changed:
    runs-on: ubuntu-latest
    outputs:
      nest-probot-changed: ${{ steps.files-changed.outputs.nest-probot_any_changed }}
      web-driver-changed: ${{ steps.files-changed.outputs.web-driver_any_changed }}
      spider-changed: ${{ steps.files-changed.outputs.spider_any_changed }}
      xss-changed: ${{ steps.files-changed.outputs.xss_any_changed }}
    steps:
      - uses: actions/checkout@v3

      - name: check if files package has changes
        id: files-changed
        uses: tj-actions/changed-files@v44
        with:
          files_yaml: |
            nest-probot:
              - packages/nest-probot/**
            web-driver:
              - packages/web-driver/**
            spider:
              - security/spider/**
            xss:
              - security/xss/**

  release-nest-probot:
    runs-on: ubuntu-latest
    needs:
      - check-component-changed
    if: needs.check-component-changed.outputs.nest-probot-changed == 'true'
    steps:
      - uses: actions/checkout@v3

      - name: configure git
        run: |
          git config user.name "${GITHUB_ACTOR}"
          git config user.email "${GITHUB_ACTOR}@users.noreply.github.com"

      - name: release
        run: |
          yarn install
          npx nx run nest-probot:lint --fix
          npx nx run nest-probot:semantic-release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

  release-web-driver:
    runs-on: ubuntu-latest
    needs:
      - check-component-changed
    if: needs.check-component-changed.outputs.web-driver-changed == 'true'
    steps:
      - uses: actions/checkout@v3

      - name: configure git
        run: |
          git config user.name "${GITHUB_ACTOR}"
          git config user.email "${GITHUB_ACTOR}@users.noreply.github.com"

      - name: release
        run: |
          yarn install
          npx nx run web-driver:lint --fix
          npx nx run web-driver:semantic-release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

  release-spider:
    runs-on: ubuntu-latest
    needs:
      - check-component-changed
      - release-web-driver
    if: needs.check-component-changed.outputs.spider-changed == 'true'
    steps:
      - uses: actions/checkout@v3

      - name: configure git
        run: |
          git config user.name "${GITHUB_ACTOR}"
          git config user.email "${GITHUB_ACTOR}@users.noreply.github.com"

      - name: release
        run: |
          yarn install
          npx nx run spider:lint --fix
          npx nx run spider:semantic-release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

  release-xss:
    runs-on: ubuntu-latest
    needs:
      - check-component-changed
      - release-web-driver
    if: needs.check-component-changed.outputs.xss-changed == 'true'
    steps:
      - uses: actions/checkout@v3

      - name: configure git
        run: |
          git config user.name "${GITHUB_ACTOR}"
          git config user.email "${GITHUB_ACTOR}@users.noreply.github.com"

      - name: release
        run: |
          yarn install
          npx nx run xss:lint --fix
          npx nx run xss:semantic-release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
